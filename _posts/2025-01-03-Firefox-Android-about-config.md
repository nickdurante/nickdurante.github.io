There is a neat little trick that I discovered today.

The `about:config` webpage in Firefox for Android
is not available on the UI but the same page
can be reached at:

```
chrome://geckoview/content/config.xhtml
```
